apiVersion: v1
kind: Service
metadata:
  name: self-healing-app-svc
  labels:
    app: self-healing-app
spec:
  type: NodePort
  selector:
    app: self-healing-app
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
